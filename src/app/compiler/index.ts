import * as ts from '../../../supported-typescript/node_modules/typescript';
import transformer from '../../../supported-typescript/node_modules/typescript-rtti/dist/transformer';
import * as lib from 'typescript-rtti';

export const Compiler = {
    ts, transformer, lib
};